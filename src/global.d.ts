declare type Post = {
  title: string,
  slug: string,
  wordCount: number,
  body: string,
  tags: string[],
  created: number
}

declare type PostStub = {
  title: string,
  slug: string,
  wordCount: number,
  tags: string[],
  created: number
}
